<<<<<<< SEARCH
  const handleExportCsv = () => {
    const csv = Papa.unparse(connections);
    const blob = new Blob([csv], { type: 'text/csv;charset=utf-8;' });
    const link = document.createElement('url');
    const url = URL.createObjectURL(blob);

    const a = document.createElement('a');
    a.href = url;
    a.download = `connections_${new Date().toISOString().split('T')[0]}.csv`;
    a.click();

    URL.revokeObjectURL(url);
  };
=======
  const handleExportCsv = () => {
    // Exclude password from the export
    const connectionsToExport = connections.map(c => {
      const { password, ...rest } = c;
      return rest;
    });

    const csv = Papa.unparse(connectionsToExport);
    const blob = new Blob(['\uFEFF' + csv], { type: 'text/csv;charset=utf-8;' }); // Add BOM for Excel
    const url = URL.createObjectURL(blob);

    const a = document.createElement('a');
    a.href = url;
    a.download = `connections_${new Date().toISOString().split('T')[0]}.csv`;
    a.click();

    URL.revokeObjectURL(url);
  };
>>>>>>> REPLACE
